import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'ps-audit-filter-rail',
  templateUrl: './audit-filter-rail.html',
  styleUrl: './audit-filter-rail.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AuditFilterRail {
}
