# Admin Dashboard — UI Bug Log

Bugs discovered while running [`protected/admin-dashboard.md`](../protected/admin-dashboard.md) against `http://localhost:4200/admin`.

Prefix: `DASH-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### DASH-NNN: <one-line summary>
- **Audit section:** <e.g., "KPI cards · Tutorials count">
- **Expected (skeleton):** <copy verbatim from protected/admin-dashboard.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
