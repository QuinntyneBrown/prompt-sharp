# About — UI Bug Log

Bugs discovered while running [`public/about.md`](../public/about.md) against `http://localhost:4200/about`.

Prefix: `ABOUT-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### ABOUT-NNN: <one-line summary>
- **Audit section:** <e.g., "Contact card · form">
- **Expected (skeleton):** <copy verbatim from public/about.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
