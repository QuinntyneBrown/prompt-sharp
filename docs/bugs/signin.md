# Sign-in — UI Bug Log

Bugs discovered while running [`public/signin.md`](../public/signin.md) against `http://localhost:4200/sign-in`.

Prefix: `SIGNIN-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### SIGNIN-NNN: <one-line summary>
- **Audit section:** <e.g., "Card head · italic slash in h2">
- **Expected (skeleton):** <copy verbatim from public/signin.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
