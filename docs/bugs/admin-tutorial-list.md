# Admin Tutorial List — UI Bug Log

Bugs discovered while running [`protected/admin-tutorial-list.md`](../protected/admin-tutorial-list.md) against `http://localhost:4200/admin/tutorials`.

Prefix: `TUTLIST-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### TUTLIST-NNN: <one-line summary>
- **Audit section:** <e.g., "Data table · Row actions">
- **Expected (skeleton):** <copy verbatim from protected/admin-tutorial-list.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
