# Admin Media — UI Bug Log

Bugs discovered while running [`protected/admin-media.md`](../protected/admin-media.md) against `http://localhost:4200/admin/media`.

Prefix: `MEDIA-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### MEDIA-NNN: <one-line summary>
- **Audit section:** <e.g., "Selection bar · Bulk actions">
- **Expected (skeleton):** <copy verbatim from protected/admin-media.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
