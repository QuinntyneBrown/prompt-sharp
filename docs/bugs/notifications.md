# Notifications — UI Bug Log

Bugs discovered while running [`protected/notifications.md`](../protected/notifications.md) against `http://localhost:4200/admin/notifications`.

Prefix: `NOTIF-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### NOTIF-NNN: <one-line summary>
- **Audit section:** <e.g., "Snackbar · success · with-action">
- **Expected (skeleton):** <copy verbatim from protected/notifications.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
