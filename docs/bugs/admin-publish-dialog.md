# Admin Publish Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-publish-dialog.md`](../protected/admin-publish-dialog.md) against `http://localhost:4200/admin/tutorials` (with the Publish dialog open).

Prefix: `PUB-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### PUB-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§3.3 Scheduled date/time · disabled state">
- **Expected (skeleton):** <copy verbatim from protected/admin-publish-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
