# Admin Users — UI Bug Log

Bugs discovered while running [`protected/admin-users.md`](../protected/admin-users.md) against `http://localhost:4200/admin/users`.

Prefix: `USERS-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### USERS-NNN: <one-line summary>
- **Audit section:** <e.g., "Users table · Role chips">
- **Expected (skeleton):** <copy verbatim from protected/admin-users.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
