# Category — UI Bug Log

Bugs discovered while running [`public/category.md`](../public/category.md) against `http://localhost:4200/categories/:slug`.

Prefix: `CATEGORY-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### CATEGORY-NNN: <one-line summary>
- **Audit section:** <e.g., "Category hero · panel">
- **Expected (skeleton):** <copy verbatim from public/category.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
