# Profile — UI Bug Log

Bugs discovered while running [`protected/profile.md`](../protected/profile.md) against `http://localhost:4200/profile`.

Prefix: `PROFILE-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### PROFILE-NNN: <one-line summary>
- **Audit section:** <e.g., "Identity card · Provider chip">
- **Expected (skeleton):** <copy verbatim from protected/profile.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
