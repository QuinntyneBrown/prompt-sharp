# Session Expired Dialog — UI Bug Log

Bugs discovered while running [`protected/session-expired-dialog.md`](../protected/session-expired-dialog.md) against any authenticated page after a 401 (with the Session-expired modal dialog open).

Prefix: `SESSION-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### SESSION-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§1 Chrome · Esc does not dismiss">
- **Expected (skeleton):** <copy verbatim from protected/session-expired-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
