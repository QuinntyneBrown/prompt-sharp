# Admin Unsaved Changes Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-unsaved-changes-dialog.md`](../protected/admin-unsaved-changes-dialog.md) against `http://localhost:4200/admin/tutorials/:id/edit` (with the Unsaved-changes guard dialog open).

Prefix: `UNSAVED-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### UNSAVED-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§4 Actions · Discard button variant">
- **Expected (skeleton):** <copy verbatim from protected/admin-unsaved-changes-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
