# Admin Confirm Delete Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-confirm-delete-dialog.md`](../protected/admin-confirm-delete-dialog.md) against `http://localhost:4200/admin/tutorials` (with the Delete confirmation dialog open).

Prefix: `DEL-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### DEL-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§4 Actions · Delete button color">
- **Expected (skeleton):** <copy verbatim from protected/admin-confirm-delete-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
