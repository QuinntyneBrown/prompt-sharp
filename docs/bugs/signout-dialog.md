# Sign-out Dialog — UI Bug Log

Bugs discovered while running [`protected/signout-dialog.md`](../protected/signout-dialog.md) against `http://localhost:4200/` (with the Sign-out dialog open, from either the public nav or the admin nav rail).

Prefix: `SIGNOUT-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### SIGNOUT-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§2 Headline · slash treatment">
- **Expected (skeleton):** <copy verbatim from protected/signout-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
