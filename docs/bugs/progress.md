# Progress — UI Bug Log

Bugs discovered while running [`protected/progress.md`](../protected/progress.md) against `http://localhost:4200/progress`.

Prefix: `PROGRESS-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### PROGRESS-NNN: <one-line summary>
- **Audit section:** <e.g., "In progress section · Row meter">
- **Expected (skeleton):** <copy verbatim from protected/progress.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
