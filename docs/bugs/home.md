# Home — UI Bug Log

Bugs discovered while running [`public/home.md`](../public/home.md) against `http://localhost:4200/`.

Prefix: `HOME-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### HOME-NNN: <one-line summary>
- **Audit section:** <e.g., "Hero · CTA buttons">
- **Expected (skeleton):** <copy verbatim from public/home.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
