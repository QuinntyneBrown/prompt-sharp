# Access Denied — UI Bug Log

Bugs discovered while running [`public/access-denied.md`](../public/access-denied.md) against `http://localhost:4200/access-denied`.

Prefix: `ACCESS-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### ACCESS-NNN: <one-line summary>
- **Audit section:** <e.g., "Access stage · error-red italic 'denied'">
- **Expected (skeleton):** <copy verbatim from public/access-denied.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
