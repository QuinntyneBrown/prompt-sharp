# Admin Media Upload Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-media-upload-dialog.md`](../protected/admin-media-upload-dialog.md) against `http://localhost:4200/admin/media` (with the Upload dialog open).

Prefix: `UPLOAD-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### UPLOAD-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§3.1 Drop zone · dashed border">
- **Expected (skeleton):** <copy verbatim from protected/admin-media-upload-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
