# OAuth Callback — UI Bug Log

Bugs discovered while running [`public/oauth-callback.md`](../public/oauth-callback.md) against `http://localhost:4200/auth/callback`.

Prefix: `OAUTH-CB-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### OAUTH-CB-NNN: <one-line summary>
- **Audit section:** <e.g., "Auth card · spinning dot">
- **Expected (skeleton):** <copy verbatim from public/oauth-callback.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
