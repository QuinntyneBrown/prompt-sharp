# Catalog — UI Bug Log

Bugs discovered while running [`public/catalog.md`](../public/catalog.md) against `http://localhost:4200/tutorials`.

Prefix: `CATALOG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### CATALOG-NNN: <one-line summary>
- **Audit section:** <e.g., "Catalog header · meta-rail">
- **Expected (skeleton):** <copy verbatim from public/catalog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
