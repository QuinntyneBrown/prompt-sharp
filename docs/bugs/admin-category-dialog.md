# Admin Category Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-category-dialog.md`](../protected/admin-category-dialog.md) against `http://localhost:4200/admin/categories` (with the Create / Edit category dialog open).

Prefix: `CAT-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### CAT-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§3.4 Color swatch picker · selected state ring">
- **Expected (skeleton):** <copy verbatim from protected/admin-category-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
