# Admin Categories — UI Bug Log

Bugs discovered while running [`protected/admin-categories.md`](../protected/admin-categories.md) against `http://localhost:4200/admin/categories`.

Prefix: `CAT-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### CAT-NNN: <one-line summary>
- **Audit section:** <e.g., "Tabs · Tags tab">
- **Expected (skeleton):** <copy verbatim from protected/admin-categories.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
