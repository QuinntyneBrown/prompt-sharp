# Admin User Invite Dialog — UI Bug Log

Bugs discovered while running [`protected/admin-user-invite-dialog.md`](../protected/admin-user-invite-dialog.md) against `http://localhost:4200/admin/users` (with the Invite dialog open).

Prefix: `INVITE-DLG-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### INVITE-DLG-NNN: <one-line summary>
- **Audit section:** <e.g., "§3.2 Role select · default selection">
- **Expected (skeleton):** <copy verbatim from protected/admin-user-invite-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
