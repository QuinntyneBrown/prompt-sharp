# OAuth Consent Dialog — UI Bug Log

Bugs discovered while running [`public/oauth-consent-dialog.md`](../public/oauth-consent-dialog.md) against `http://localhost:4200/auth/consent`.

Prefix: `OAUTH-CONSENT-`. Status values: `open` · `in-progress` · `resolved` (record commit SHA when resolved).

---

<!--
Template — copy this block for each new bug.

### OAUTH-CONSENT-NNN: <one-line summary>
- **Audit section:** <e.g., "Consent card · scope list">
- **Expected (skeleton):** <copy verbatim from public/oauth-consent-dialog.md>
- **Actual (app):** <what the app actually shows>
- **Screenshot:** <optional file path>
- **Fix location:** <component path>
- **Status:** open
- **Resolved by:** <commit sha — when status flips to resolved>

-->

_No bugs logged yet — run the audit and append entries here._
